@{
	var userId = User.Identity.IsAuthenticated ? Context.User.Identity.Name : "anonymous";
}
<button id="account-dropdown-button" data-type="dropdown" data-target="#account-dropdown" class="btn-dropdown btn-account">
	<img class="account-img" src="~/ProjectImages/Icons/Avatar.svg" alt="Avatar icon." />
	<div id="account-dropdown" class="card dropdown-container account-dropdown-container">
		<div class="account-dropdown">
			<div class="dropdown-header">
				@* User picture
				user full name  *@
			</div>
			<div class="dropdown-line"></div>
			<div class="options-container">
				<div class="option-item" onclick="">
					<i class="fa-solid fa-gear"></i>
					<p>Dark mode</p>
					<div>Darkmode toggle</div>
				</div>
				<div class="option-item" onclick=""><span class="fa-solid fa-right-from-bracket"></span>Log Out</div>
			</div>
		</div>
	</div>
</button>